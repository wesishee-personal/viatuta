import type { MaplibreProps } from "../maplibre/maplibre.js";
import type { ViewState } from "../types/common.js";
import type { TransformLike } from "../types/internal.js";
import type { MapInstance } from "../types/lib.js";
/**
 * maplibre-gl v6 removed the public `map.transform` property in favor of
 * discrete getters. Reconstruct a TransformLike snapshot from those getters
 * so it works across maplibre-gl v4/v5/v6.
 */
export declare function getTransformLike(map: MapInstance): TransformLike;
/**
 * Capture a transform's current state
 * @param transform
 * @returns descriptor of the view state
 */
export declare function transformToViewState(tr: TransformLike): ViewState;
/**
 * Applies requested view state to a transform
 * @returns an object containing detected changes
 */
export declare function applyViewStateToTransform(
/** An object that describes Maplibre's camera state */
tr: TransformLike, 
/** Props from Map component */
props: MaplibreProps): Partial<TransformLike>;
export declare function updateZoomConstraint(map: MapInstance, nextRange: {
    min: number;
    max: number;
}, currentRange: {
    min: number;
    max: number;
}): boolean;
export declare function updatePitchConstraint(map: MapInstance, nextRange: {
    min: number;
    max: number;
}, currentRange: {
    min: number;
    max: number;
}): boolean;
//# sourceMappingURL=transform.d.ts.map